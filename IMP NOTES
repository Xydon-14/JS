1) Multiple if's can be executed if condition is true
2) Only one else if can be executed i.e the first one

Strict Equality (===) : checks the type and equality 2==="2" = false

for of : returns value (applicable for map)
for in : returnd index
forEach() : does not return anything

ways to define function
1) function func_name (parameter){}
2) cosnt func_name = (parameter)=>() for implicity return,()=>{} for explicit return

map = stores the sequence of the elements and keys should be unique

Can we use implicit return in normal function